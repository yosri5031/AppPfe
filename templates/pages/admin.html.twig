{% extends 'base.html.twig' %}
{% block body %}
<!-- inclusion du fichier css dans ma page twig -->
<style>{% include 'css/admin.css' %}</style>
<div class="row no-gutter">
        <!-- The image half -->
        <div class="col-md-6 d-none d-md-flex bg-image">
        
        </div>


        <!-- The content half -->
        <div class="col-md-6 bg-light">
            <div class="login d-flex align-items-center py-5">

                <!-- second half content-->
                
                <div class="container">
                    <div class="row">
                        <div class="col-lg-10 col-xl-7 mx-auto">
                            <h3 class="display-4">Espace Admin!</h3>
                            <p class="text-muted mb-4">accée exclusive  pour les enseignants et les administrateurs.</p>
                           
                            <form action="{{ path('app_login') }}" method="POST"> 
                            
                            {% if error %}
                              <div class="alert alert-danger">{{ error.messageKey|trans(error.messageData, 'security') }}</div>
                                {% endif %} -
                              
                             {% if app.user %}
                                     <div class="mb-3">
                                    You are logged in as {{ app.user.username }}, <a href="{{ path('app_logout') }}">Logout</a>
                                      </div>
                                    {% endif %} 
                                <div class="form-group mb-3">
                                    <input id="inputEmail" type="login" placeholder="login" name="username" value="{{ last_username }}" class="form-control rounded-pill border-0 shadow-sm px-4">
                                </div>
                                <div class="form-group mb-3">
                                    <input id="inputPassword" type="password" placeholder="Password" name="password" class="form-control rounded-pill border-0 shadow-sm px-4 text-primary">
                                </div>
                                <!--<div class="custom-control custom-checkbox mb-3">
                                    <input id="customCheck1" type="checkbox" checked class="custom-control-input">
                                    <label for="customCheck1" class="custom-control-label">Remember password</label>
                                </div> -->
                                
                                <button type="submit" class="btn btn-primary btn-block text-uppercase mb-2 rounded-pill shadow-sm">Sign in</button>
                                <div class="text-center d-flex justify-content-between mt-4"><p>Snippet by <a href="https://bootstrapious.com/snippets" class="font-italic text-muted"> 
                                        <u>Boostrapious</u></a></p></div>

                            </form>
                        </div>
                    </div>
                </div><!-- End -->

            </div>
        </div><!-- End -->

    </div>
</div>


			<!-- <div class="container p-3 my-3 bg-primary text-white">

				<h2>
					Espace Admin/Ensignant
				</h2>
				<p>
					cette page permet aux enseignants/administrateurs a accéder aux analyse des enquetes remplie par les étudiants des différents niveaux.
				</p>
				
			</div>
		

<!-- login div  -->
<!--<div class="login_section_externe">
  <!-- <p class="valid">Valid. Please wait a moment.</p> -->
  <!-- <p class="error">Error. Please enter correct Username &amp; password.</p> -->
  <!--<form class="loginbox" autocomplete="off">
   <h2> Admin login </h2>
    <input placeholder="Username" type="text" id="username"></input>
    <input placeholder="Password" type="password" id="password"></input>
<button id="submit">Login</button>
</form>
</div> -->
{% endblock %}
{% block title %} Admin{% endblock %}